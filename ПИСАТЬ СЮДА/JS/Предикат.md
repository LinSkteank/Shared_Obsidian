Это функция которая возврящает bool'евое значение (либо true, либо false).

### Пример
-   `isInfant()` — «младенец ли?»
-   `hasChildren()` — «есть ли дети?»
-   `isEmpty()` — «пустой ли?»
-   `hasErrors()` — «есть ли ошибки?»